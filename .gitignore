node_modules/
.vite/
dist/
*.tsbuildinfo

# Source maps are build output; the bundle they belong to is committed so the
# site can be served straight from the repository, but the maps are large and
# nobody debugging production needs them in version control.
assets/*.map
assets/chunks/*.map

.DS_Store
Thumbs.db
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

.claude/

_t/
*.scratch.*

# Serena code-intelligence cache (project.yml is committed, cache is not)
**/.serena/cache/
